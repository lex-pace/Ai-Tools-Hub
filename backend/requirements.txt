# ============================================================
# AI Tools Hub — 后端 Python 依赖
# ============================================================

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# 数据库
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.0
greenlet==3.0.3

# Elasticsearch
elasticsearch[async]==8.15.0

# Redis
redis==5.0.8

# 向量数据库 (嵌入式)
chromadb==0.5.0

# HTTP 客户端 (调用外部 API)
httpx==0.27.0

# 配置管理
pydantic-settings==2.5.0
python-dotenv==1.0.1

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# 数据验证
pydantic==2.9.0
email-validator==2.2.0

# 工具
python-slugify==8.0.4
markdown==3.7

# 本地 Embedding (可选，离线模式)
sentence-transformers==3.1.0
